# Copie este archivo a .env y complete los valores.
# ⚠ NO ponga aquí claves reales: este archivo se versiona y es público.
#   Las claves reales van en .env (ignorado por git).
POSTGRES_DB=auxiliar_contable
POSTGRES_USER=auxiliar
POSTGRES_PASSWORD=CAMBIE_ESTA_CLAVE
# Genere uno con: openssl rand -base64 48
JWT_SECRET=CAMBIE_ESTE_SECRETO
